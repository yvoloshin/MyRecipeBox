<div class = "booyah-box col-xs-10 col-xs-offset-1">
	<% @recipes.each do |recipe| %>
		<div class = "booyah-box col-xs-10 col-sm-5 col-xs-offset-1">
			<h1><%= link_to recipe.name, recipe_path(recipe) %></h1><br>
			<%= recipe.description %></br>
		</div>
	<% end %>
</div>
